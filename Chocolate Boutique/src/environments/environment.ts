// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCKQfF3we3sez_0nXO37L-tmrDAF_odXvI",
    authDomain: "choco-store.firebaseapp.com",
    databaseURL: "https://choco-store.firebaseio.com",
    projectId: "choco-store",
    storageBucket: "choco-store.appspot.com",
    messagingSenderId: "112935280562",
    appId: "1:112935280562:web:b8d166b30cdba13c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
